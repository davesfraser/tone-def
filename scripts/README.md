# Scripts

Use this directory for checked-in developer or maintenance scripts.

Prefer scripts that are repeatable, documented, and safe to run from a clean
checkout. Put reusable application logic in `src/` instead.
